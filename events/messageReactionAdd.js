module.exports = async (messageReaction, user) => {/*
    if (reaction.message.id == "768966138188005408") {
        guild.fetchMember(user) // fetch the user that reacted
            .then((member) => {
                let role = (user.roles.add("767763877072208002"));
                role
                    .then(() => {
                        console.log(`Added the role to ${member.user.username}`);
                    }
                    );
            });
    }*/
};